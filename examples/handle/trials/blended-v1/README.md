# Blended thumb developer proof

An additive refinement against exact actual provider initial STEP5284ab86 passed all nine unchanged checks. The thumb lobe uses tangent concave planform arcs and shares the arched grip thickness. Source, STEP and STL retain exact observed bytes; structured result paths are relocated while measurements and check-bundle hash remain unchanged. Baseline acceptance is synthetic for this proof. This does not record an application refinement or user acceptance.

The original executed command was node --import tsx .runtime/tools/prove_actual_blend.ts in the TOOLS worktree. The portable runner below uses the saved inputs and same adapter; it was typechecked but not rerun after packaging:

```sh
node --import tsx src/tools/prove_handle_blend.ts examples/handle/trials/blended-v1
```

Measured: zero removed material, at least4mmwidth increase,5mmthumb protrusion increase,562.5083mm3outboard addition and79250triangles. Export verification took28.6189s of the30s stage allowance; timing margin is narrow. Geometry checks do not establish physical comfort, strength or fit. Appearance review is separate.
