# Numeric core fixture trial

These files are actual outputs of the TOOLS-02 internal CLI for a fixed 36 mm
plate fixture. They are not application output, an accepted design or a physical
test. `length-36/result.json` records all seven checks, hashes and four removed
container identities. The delivered Python source was independently regenerated.

The [trial fixture](../../../../fixtures/tools/numeric-core-trial.json) records
execution provenance, source-code hashes and actual results for lengths 50, 30
and 36 mm. The 30 mm fixture failed its measured 2 mm end margin; it was not
silently resized. Parent wrapper verification receipts remain separate.
