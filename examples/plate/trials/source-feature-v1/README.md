# Fixed-source tactile feature trial

Actual isolated execution of the synthetic capsule source from the preregistered
TOOLS-03 acceptance test. This is developer input, not a product Responses
generation or model-repair claim. All nine frozen checks passed.

The candidate adds 73.13274122871944 mm3 with measured spans
10 x 4 x 2 mm. Total bounds are [0,50] x [0,35] x [0,7];
the preserved base is 5 mm thick. Both bores remain unobstructed.

- [source.py](source.py) and [editable.py](editable.py): exact same executed UTF-8 bytes.
- [candidate.step](candidate.step) and [preview.stl](preview.stl): sealed STEP and trusted exported mesh, in mm.
- [result.json](result.json): exact v2 adapter result with its original private paths.
- [core-result.json](core-result.json): actual stage identities, hashes, measurements and confirmed removal.
- [hashes.json](hashes.json): hashes for the saved byte-for-byte copies and regeneration.

Original private artifact paths in the result are historical execution addresses
and may expire. Use the relative files and hash manifest for this saved package.
No source or geometry was regenerated while copying it. The independent
regenerator's STEP hash is retained; its geometric symmetric difference was zero.

No user acceptance, physical test, native CAD history, browser integration or
application artifact delivery is established. The parent owns development
execution receipts and commits. Historical trials remain unchanged.

This snapshot was captured before the final feature-tunnel mesh refinement.
The stage input hashes in `core-result.json` identify the exact verifier files
used for this observation; final acceptance is separate parent-owned evidence.
