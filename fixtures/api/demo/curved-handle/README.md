# Recorded approved handle

These are actual accepted files from the local September8,2026 handle run, not synthetic fixtures or a new live run. `saved-demo.json` preserves both original acceptance/manifest pairs and maps their eight CAD artifacts to static public URLs. The original manifest URLs still describe the recorded API; consumers use the separate static mapping without rewriting the evidence.

Initial revision `revision_cfa10ddf-aab7-462f-9d6d-7013dd92a9e3` passed eight checks and was explicitly approved. Refined revision `revision_e1ec294f-13df-4e88-a32a-cfbf631120b2` passed nine and was explicitly approved. Source runtime commit: `5be4b7a5c8211a8eb4b537c20bc60766378cce32`. Units are millimeters. The requirements remain sample mounting pitch96mm, minimum finger gap25mm and maximum length140mm. Actual both-stage length110mm and minimum gap29.568014623159286mm; initial center grip10mm, refined center grip19.0000002mm. Overall bounds include the mounting pads and must not be labeled grip width.

`prototype.zip` is the unchanged18-entry final package, SHA256 `7f11b9fa4d2e52c735746ec22f202f563f396024ce32675b50557d69d7e7dcfa`. It includes exact source, editable Python, STEP, STL, accepted initial STEP dependency, reference/datums and original acceptance/check evidence. Optional RFQ fields are all unknown, and nothing was submitted to a supplier. Use the full ZIP to preserve editable-source dependencies.

The build verifies acceptance relationships and every file hash before copying only the nine named files plus JSON index into `/demo/handle/`. No API, invitation, provider request or CAD execution is needed to view them. Raw provider receipts, runtime logs and private coordination documents are excluded. The package and metadata were reviewed for local paths and credentials before publication.

This is a read-only recorded demonstration. Physical fit, comfort, strength, mounting hardware and manufacturing readiness were not tested. Selecting it does not make its evidence current for a custom live design.
